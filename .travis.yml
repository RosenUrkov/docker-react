sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t rosenurkov/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rosenurkov/docker-react npm run test -- --coverage
